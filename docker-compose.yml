version: '3'
services:
  producer:
    build: producer
    depends_on:
      - rmq
  consumer:
    build: consumer
    depends_on:
      - rmq
  crl:
    build: crl
    hostname: crl-server
    # Note: handy for debugging
    # ports:
    #   # HOST:CONTAINER
    #   - 8000:8000
  rmq:
    build: rmq
    hostname: rabbitmq
