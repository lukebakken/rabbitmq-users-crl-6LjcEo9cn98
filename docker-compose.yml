version: '3'
services:
  producer:
    build: producer
    depends_on:
      - rmq0
  consumer:
    build: consumer
    depends_on:
      - rmq1
  crl:
    build: crl
    hostname: crl-server
    # Note: handy for debugging
    # ports:
    #   # HOST:CONTAINER
    #   - 8000:8000
  rmq0:
    build: rmq0
    hostname: rmq0
  rmq1:
    build: rmq1
    hostname: rmq1
